#ifndef UTIL_ARRAYS_H_
#define UTIL_ARRAYS_H_

// Method to verify that an array is sorted in ascending order
bool IsSorted(int* array, int length);

// Prints contents of array
void PrintArray(int* array, int length);

// Generates an array of size n with random integers
int* GenerateArray(int n);

#endif
